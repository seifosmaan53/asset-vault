FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Expose Vite dev server port
EXPOSE 5173

# Start dev server (command will be overridden by docker-compose)
CMD ["npm", "run", "dev"]

